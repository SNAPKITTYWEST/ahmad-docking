[toolchain]
channel    = "stable"
components = ["rustfmt", "clippy", "rust-src", "llvm-tools-preview"]
targets    = ["x86_64-unknown-linux-gnu", "wasm32-wasip1"]

[profile]
dev     = { opt-level = 0, debug = true }
release = { opt-level = 3, lto = "fat", codegen-units = 1, strip = true }
